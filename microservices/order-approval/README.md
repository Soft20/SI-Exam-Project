# Approval service

Add a `.env` file to the root of the `approval service` project.

```env
HOST=localhost
PORT=5003
NAME=order-approval

# eureka configuration
EUREKA_HOST=localhost
EUREKA_PORT=8761
```